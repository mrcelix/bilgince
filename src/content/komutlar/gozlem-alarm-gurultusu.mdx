---
baslik: "En çok tetiklenen alarmlar"
ozet: "Otuz günde en çok çalan alarmları sayar; alarm yorgunluğunun kaynağı ilk üç satırdadır."
konu: "gozlemlenebilirlik"
etiketler: ["izleme", "alarm"]
kod: |
  curl -s "$PROM/api/v1/query?query=sort_desc(
    count_over_time(ALERTS{alertstate=\"firing\"}[30d])
  )" | jq -r '.data.result[] | "\(.value[1])\t\(.metric.alertname)"' | head -15
dikkat: "$PROM Prometheus adresiniz. 30 günlük sorgu büyük kümelerde yavaş çalışır, saatte bir çalıştırmayın."
---

Komutun ne yaptığını okumadan çalıştırmayın. Çıktıyı bir kez üretim dışı bir makinede görün, sonra sahada kullanın.
