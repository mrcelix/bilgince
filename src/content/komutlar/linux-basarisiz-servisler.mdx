---
baslik: "Başarısız systemd servisleri ve son hataları"
ozet: "Hangi servisin düştüğünü ve düşerken ne yazdığını tek çıktıda verir."
konu: "linux"
etiketler: ["linux", "systemd"]
kod: |
  systemctl --failed --no-legend | awk '{print $1}' | while read -r s; do
    echo "=== $s"
    journalctl -u "$s" -n 5 --no-pager -p err
  done
dikkat: "journalctl kalıcı değilse yeniden başlatma sonrası geçmiş kaybolur: `Storage=persistent`."
---

Komutun ne yaptığını okumadan çalıştırmayın. Çıktıyı bir kez üretim dışı bir makinede görün, sonra sahada kullanın.
