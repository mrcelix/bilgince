---
baslik: "Terraform durum sürüklemesi"
ozet: "Koddan sapmış kaynakları değişiklik uygulamadan gösterir."
konu: "otomasyon"
etiketler: ["terraform", "iac"]
kod: |
  terraform plan -refresh-only -detailed-exitcode -no-color |
    grep -E '^\s+[~+-]' | head -40
dikkat: "`-detailed-exitcode` fark varsa 2 döner; CI adımınız 0 dışındaki her çıkışı hata sayıyorsa bunu ayrıca ele alın."
---

Komutun ne yaptığını okumadan çalıştırmayın. Çıktıyı bir kez üretim dışı bir makinede görün, sonra sahada kullanın.
