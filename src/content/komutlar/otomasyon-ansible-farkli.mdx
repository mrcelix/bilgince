---
baslik: "Ansible: hangi sunucular hedeften sapmış?"
ozet: "Kuru çalıştırma ile değişecek makineleri listeler; uygulamadan önce kapsamı gösterir."
konu: "otomasyon"
etiketler: ["ansible", "iac"]
kod: |
  ansible-playbook site.yml --check --diff |
    awk '/^changed:/ {print $2}' | tr -d '[]' | sort -u
dikkat: "Kuru çalıştırma bazı modüllerde yanlış \"değişecek\" der (özellikle command/shell). `changed_when` tanımlayın."
---

Komutun ne yaptığını okumadan çalıştırmayın. Çıktıyı bir kez üretim dışı bir makinede görün, sonra sahada kullanın.
